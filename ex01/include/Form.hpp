#ifndef FORM_HPP
# define FORM_HPP

#include <exception>
#include <string>
#include <iostream>

class Form {
    private:
        const std::string _name_;
        bool is_signed;
        const int gradeSigned;
        const int gradeExecute;
    public:
        
}